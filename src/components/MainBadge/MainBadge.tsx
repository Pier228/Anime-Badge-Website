import MainBadgeHeader from "./MainBadgeHeader";
import styles from "@/styles/badge/main-badge.module.scss";
const Main = () => {
    return (
        <div className={styles.container}>
            <MainBadgeHeader />
        </div>
    );
};

export default Main;
